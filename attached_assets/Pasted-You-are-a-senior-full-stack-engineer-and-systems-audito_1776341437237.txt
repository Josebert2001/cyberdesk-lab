You are a senior full-stack engineer and systems auditor working inside this project in Replit.

Your task is to fix errors, remove broken or misleading parts, and upgrade the app based strictly on the audit below. Do not just explain what is wrong — actually inspect the codebase, correct issues, and implement the missing pieces in a clean, production-ready way.

PROJECT AUDIT SUMMARY
This project is a Cybersecurity Department Portal for the University of Uyo.

Current audit findings:
1. React 18 SPA built with Vite, TypeScript, Tailwind CSS
2. Uses React Router v6 across multiple pages
3. Includes cybersecurity labs, AI tutoring, exam prep, roadmaps, opportunities, staff, showcase, etc.
4. Uses shadcn/ui and Radix UI components
5. Context API is used for AI panel visibility and XP/ranking system
6. TanStack React Query is installed but currently unused
7. Gemini 1.5 Pro API is used for:
   - Lab analysis panel
   - AskAnything chat tutor
8. Expected env variable: VITE_GEMINI_API_KEY
9. XP, exam notes, and bookmarked opportunities are stored only in localStorage
10. Supabase client is configured, but:
   - no tables exist
   - no migrations exist
   - no edge functions exist
   - no secrets exist
   - schema is empty
11. No authentication system exists, even though login/signup links appear in the UI
12. Testing is almost empty
13. ESLint and TypeScript are configured
14. App is deployable as static frontend, but backend capability is not actually used

MAIN GOAL
Fully audit the current codebase and fix all errors or misleading architecture issues exposed by this report. Then upgrade the project so it becomes functionally correct, consistent, and ready for deployment.

IMPORTANT EXECUTION RULES
- First inspect the existing code before making assumptions
- Fix actual code errors, broken imports, bad routes, invalid state handling, and misleading UI
- Do not leave fake login/signup links if auth is not implemented
- Do not keep dead integrations that are not used unless you properly integrate them
- Keep the UI modern and clean
- Preserve existing good features where possible
- Do not break working pages
- Prefer production-grade structure, not hacks
- Add comments only where truly helpful
- If environment variables are missing, make the app fail gracefully with clear developer feedback
- Make sure the final app can run successfully in Replit

IMPLEMENTATION TASKS

PHASE 1 — FULL CODEBASE INSPECTION
1. Inspect the entire codebase for:
   - TypeScript errors
   - broken imports
   - invalid component references
   - routing errors
   - incorrect page links
   - dead code
   - unused libraries
   - inconsistent naming
   - missing env handling
   - bad state logic
   - UI elements that suggest features that do not exist
2. Fix all compile-time and runtime issues
3. Clean up unused or misleading code

PHASE 2 — FRONTEND CONSISTENCY FIXES
1. Verify all pages render correctly
2. Verify React Router navigation works correctly
3. Fix broken buttons, empty states, and placeholder actions
4. If login/signup links exist but auth is not implemented, do one of the following:
   - either fully implement auth with Supabase
   - or remove/replace those links with honest messaging like “coming soon”
5. Ensure all localStorage features work reliably:
   - XP persistence
   - rank calculation
   - exam notes
   - bookmarks
6. Add safe fallbacks for corrupted localStorage data

PHASE 3 — GEMINI INTEGRATION HARDENING
1. Locate all Gemini API usage
2. Verify the app safely handles missing VITE_GEMINI_API_KEY
3. Add proper error handling for:
   - missing key
   - failed response
   - rate limits
   - malformed output
4. Ensure the UI shows meaningful user-friendly errors instead of crashing
5. Centralize Gemini logic into a reusable service if not already done
6. Prevent exposing unnecessary implementation details in the UI

PHASE 4 — SUPABASE DECISION AND IMPLEMENTATION
Supabase is currently connected but unused. Fix this architectural inconsistency properly.

Choose the best option based on the codebase:

OPTION A — FULLY INTEGRATE SUPABASE
If the project clearly intends to support real users and persistent student data, then:
1. Implement Supabase authentication
   - sign up
   - sign in
   - sign out
   - session persistence
2. Create a proper database schema for:
   - profiles
   - XP/progress
   - exam notes
   - bookmarked opportunities
3. Add migrations
4. Connect frontend state to Supabase for authenticated users
5. Sync or migrate existing localStorage data into user accounts when appropriate
6. Keep localStorage only as guest fallback if needed

OPTION B — REMOVE FALSE BACKEND SIGNALS
If full backend implementation would be excessive for the current app state, then:
1. Remove or disable misleading Supabase-dependent messaging
2. Remove fake auth links
3. Clean unused Supabase code
4. Make the app honestly operate as a local-first educational portal
5. Leave a clear developer note describing how Supabase can be added later

Pick the option that produces the most honest, stable, and maintainable result.

PHASE 5 — REACT QUERY CLEANUP
1. Inspect whether TanStack React Query is truly needed
2. If needed, integrate it properly for async AI or backend requests
3. If not needed, remove unused setup and dependencies cleanly

PHASE 6 — TESTING
1. Add meaningful tests for important logic
2. At minimum include tests for:
   - XP/rank calculation
   - localStorage persistence helpers
   - route rendering sanity checks
   - Gemini service fallback/error handling
3. Replace the placeholder example test with real tests
4. Make tests pass

PHASE 7 — DX, ENV, AND DEPLOYMENT READINESS
1. Verify `.env` expectations are clearly documented
2. Add an `.env.example` if missing
3. Ensure missing configuration does not crash the whole app without explanation
4. Verify build works
5. Verify linting/typecheck works
6. Make sure the project runs properly in Replit
7. Add concise setup instructions in README if needed

EXPECTED OUTPUT FORMAT
When you finish, provide:

1. WHAT YOU FOUND
- list of actual problems discovered in the codebase

2. WHAT YOU FIXED
- list of files/components/features fixed

3. WHAT YOU IMPLEMENTED
- especially whether you chose:
  - OPTION A: fully integrated Supabase
  - or OPTION B: removed misleading backend/auth expectations

4. REMAINING RISKS
- anything still requiring manual secrets, Supabase dashboard setup, or deployment action

5. FINAL VERIFICATION
- confirm whether:
  - app runs
  - build passes
  - typecheck passes
  - tests pass

CODING STANDARD
- Use TypeScript properly
- Keep components modular
- Prefer reusable utilities/services
- Maintain clean folder structure
- Avoid unnecessary complexity
- Do not invent features that are not justified by the app
- Make the app truthful: no fake backend, no fake auth, no fake persistence claims

Now start by scanning the current codebase and implementing the fixes directly.